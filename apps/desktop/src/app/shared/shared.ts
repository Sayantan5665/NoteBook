/**
 * Barrel export for shared components, directives, and pipes.
 *
 * Add exports here as shared UI building blocks are implemented in
 * subsequent phases (button, dialog, spinner, toast, empty-state, etc.)
 * per docs/01-architecture/05-Angular.md §3 shared/ structure.
 */
export {};
